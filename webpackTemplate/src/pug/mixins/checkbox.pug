mixin checkbox(className, text, inputId, name, checked=false)
    .checkbox(class=className)
        if checked
            input.checkbox__input(type="checkbox" id=inputId name=name checked)
        else 
            input.checkbox__input(type="checkbox" id=inputId name=name)
        label.checkbox__label(for=inputId)
            .checkbox__text!= text
